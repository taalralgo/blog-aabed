{% extends 'admin.html.twig' %}


{% block body %}
    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
{#            <tr>#}
{#                <th>Password</th>#}
{#                <td>{{ user.password }}</td>#}
{#            </tr>#}
        </tbody>
    </table>

{#    <a class="btn btn-primary" href="{{ path('user_index') }}">Liste des utilisateurs</a>#}

    <a style="margin-top: 10px; margin-bottom: 20px;" class="btn btn-warning" href="{{ path('user_edit', {'id': user.id}) }}">Modifier</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
